average-case complexity shows how program act when we enter random elements.

difference between amortized. In average-case elements are random, and tells us High probability answer. in amortized it doesnt matter how elements are in, it tells guaranted answer. 

amortized time complexity - is when we count done operations sum and devide on n.it gives us avg n price. in practical we do many small and one big case.big case is rare, demand many time and energy but with small cases we have saved both so at least it doest get big case.
Describe amortized is 3 methods . aggregate(total cost of n operations/n), accounting(saved many,can buy things) , potential(mathematic function shows saved energy)
good for shows real guaranted picture,for structure optimization, safe for hacking(if user enter bad data,it will still work fast)
 its safe fromhacking becausemathematical guarentee based on total numbers of operations,no matter inputs.attacker cant slow down server,cant bypass fast operations to slow one.slow case is mathematically locked.



memoization its saved functions answer in cache and used it for next operation.its for optimization, to get fast answer(time complexity),clean code. bad for when we have millions of cache its space complexity. use when fucntion takes many time and repetitive.
example in web: when user click check weather in london, app has to make request to intenet.if many user click same at same time it will be slower to show them result.with memoization cache will save request for 15 minutes and other many user get answer quicker.to avoid memory crash we use LRU (Least Recently Used) Cache.






dynamic programing  - DP


dp- technique,breaking into smaller, sub-problems.used for performance
when we need optimal choice(when program repeating calculations)
Overlapping Sub-problems - using cache/table
Optimal Substructure- calculating small things,will calculate big thing.(use formula/transition loop)
State Transition Equation - simply a formal mathematical formula that explains how to solve a larger problem using the answers from smaller, already-solved sub-problems.

top-down(memoization)  - you start at top big problem and break it down recursively. (recursion+cache dict)
using call stack + cache.(call stack - running function and where it saved / cache where answers are saved)
memory O(n)
minus - in python recursion has limit, if n is so big like 20000, top down crash program. (`RecursionError: maximum recursion depth exceeded`.) for not crash use Bottom-Up (Tabulation) it crash because for fib(20000) if 20000 call stack and it make server slower.and tabulation's time there is one running loop.
use when we need all subproblems.



Bottom-Up (Tabulation) - start from bottom.avoid recursions and use loops,fill table/array stepbystep.
memory O(n) or O(n^2) depend on problem.
speed fast.use when speed is critical and we need all subproblems.



do examples of dp


divide and conquer - make big problems into smalls, find each and combine.
divide - conquer(find with recursion) - combine
difference from dp subproblems doesnt repeats, cache(memoization) isnot needed

- `# 1. Base Case (1 or 0 elements left)`

- `# 2. Divide (Find mid or pivot)`

- `# 3. Conquer (Recursive calls)`

- `# 4. Combine (The logic to merge results)`

Decrease and Conquer reduces the problem down to only one smaller piece. You throw the rest away.









lru
url cahce - we want save url for a while,same command doesnt run.Caching saves the response/data of a URL.

generator - function use yields and return meaning one by one(etapobrivad abrunebs monacemebs)

practice, keywords,next stop iteration 
cutom generator 

decorator - function which uses other functions act without changing his code.for autorizations(funqciis gapartoeba)
decorator - modify or extend behavior of functions,method or class without permanently changing source code


decoratorit lsitshi luwebs amoagdebs

import functools

1)@functools.cache                   functools.lru_cache
decorator 
memorize functions previous input and output.
use case : heavy math or slow recursions / When pulling unchanged data from an API/URL repeatedly.

Difference: `@cache` has no limits,grow forever and remember every single
 unique value until program stop.risk of crash, milions of inouts. fast 
@functools.lru_cache(maxsize) has fixed size limits.when its full evicts
 leas recently used(lru) and make room for new data. no crash safely manages  and recycles memory. slow



2) @functools.wraps 

every functions has identity.own name(__name__) and documentation (__doc__) . metadata is crucial for debbuging,  logging.
def method(n): 
  @functools.wraps(n)


3)@functools.partial  ??
tool freezes some of the arguments of an existing function, creating a new, simpler version of it.
use case: 
example(
def send_request(host, api_key, endpoint, data):
  print(f"Sending to {host}/{endpoint}...")

 local_api = partial(send_request, "https://api.mycompany.com", "SECRET_KEY_123")
local_api("users", {"name": "Gio"})
local_api("orders", {"id": 99})
local_api("products", {"sku": "ABC"})
)




functools built in module for fucntion which get other functions as argument


decorator fucntion which takes another function to input,
wraps with extra behavior and return new function,without changing original code.


@staticmethod - method inside class that doesnt need self.it like function
inside class
@classmethod - method that receives class(cls) itself instead of instance(self)
@functools.lru_cache has fixed size limit(masxsize). when its full evicts lru and make room
for new data.no crash safeley manages and recycles memeory
@functools.wraps - used inside custom decorator to preserve original functions (__name__),
(__doc__) name/docstring.metadata.



args - becomes tuple of positional arguments ("luka", 22) nebismieri raodenobis positive argument becomes tuple
kwargs - becoms dictionary whatever keyword arguments {"age":22} nebismieri raodenobis keyword goes to dict

if name == __main__ ????????? how it works

generator - functions pauses at each yield,gives one value,remember where stopped
and continue from that spot next time.

iterator nawil nawil abrunebs pasuxebs
every generator is iterator, not every iterator is not generator

iterator has __iter__(self): return self and __next__(self): returns value where it stops

stopiteration is exception  whena automaticly raise(agdebs), when iterator has no other value.

iterable aris obieqti romelsac aqvs __iter__() methodi (list,str,dict)
yvelaperi rac for loopshi gavatarebt.  parent of iterator


return  - function end complitely,one value
yield - function pauses, gives one value, resume

lazy evaluation - values are computed one time,on demand.

yield from ???



iteratorze did dataze generatorit gadavuaro


klasi -data - 

qalaqi-qucha for loop - procesi  generatorit gavaketo   mteli data ertianad roar daaloados 

